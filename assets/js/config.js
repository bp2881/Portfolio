/* Fill these in after you create the Appwrite project and bucket.
   Leave endpoint empty to run entirely from the baked-in snapshot. */
window.__CONFIG__ = {
  endpoint: "",          // e.g. "https://fra.cloud.appwrite.io/v1"
  projectId: "",         // your Appwrite project ID
  bucketId: "portfolio", // public-read bucket that holds snapshot.json
  snapshotFileId: "snapshot"
};
